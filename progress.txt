## Codebase Patterns
- ESM modules with `.js` extensions in imports
- Config via env vars with defaults in `config.ts` using `env()`, `envInt()`, `envBool()` helpers
- SQLite via `better-sqlite3`; schema in `initDb()` with `ensureTableColumn()` for migrations
- CLI commands dispatched in `cli.ts` via switch/case on first arg
- Slash commands on the single `/pi` command with subcommands
- Tests in `test/` directory using vitest; test files match `*.test.ts`
- All Discord channel JIDs use `dc:<channelId>` format
- Per-channel serial processing enforced by `activeChannels` Set in `queue.ts`
- `AbortController` per active task stored in `activeTaskControllers` Map keyed by rowid
- Session directories under `config.sessionsDir`, archived with `__archived_<timestamp>` suffix
- `startGateway()` in `index.ts` orchestrates lifecycle: initDb → startDiscord → startProcessingLoop → startMediaCleanup
- Setup wizard uses readline/promises for interactive prompts
---

## 2026-03-31 15:32 - US-001: Channel access policy
- Added `channelPolicy` (open / open-trigger / allowlist) and `excludedChannels` to config.ts
- Guild auto-register in discord.ts with early exclusion check
- Setup wizard channel policy step in setup.ts
- `buildConfigFile` outputs CHANNEL_POLICY and EXCLUDED_CHANNELS
- 3 new tests in test/channel-policy.test.ts
- Files changed: src/config.ts, src/discord.ts, src/setup.ts, test/channel-policy.test.ts
- **Learnings:** The `RegisteredChannel` type import was needed in discord.ts for the auto-register block
---

## 2026-03-31 15:36 - US-002: Abort / cancel command
- Added `activeChannelControllers` Map<string, AbortController> in queue.ts for jid-based lookup
- Exported `abortChannelTask(jid)` — aborts controller + clears pending messages
- Added `/pi stop` subcommand in slash-commands.ts
- New test in test/abort.test.ts — covers no-active-task path with in-memory DB
- Files changed: src/queue.ts, src/slash-commands.ts, test/abort.test.ts
- **Learnings:** DB must be initialized for `clearPendingMessages` — test uses DB_PATH=':memory:'
---
